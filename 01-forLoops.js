// for(inicializador; condicion; incrementador) {
//     código a realizar
// }

for (let i = 0; i < 5; ) {
  console.log(`hola desde JS ${i}`);
  i++;
} 
//creo la variable, chequeo la condición, si es true ejecuto el código, incremento, chequeo condición, si es true ejecuto código, incremento, etc
